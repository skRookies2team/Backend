# ==============================================
# AWS S3 설정
# ==============================================
# 버킷 이름 (전 세계에서 유니크해야 함)
AWS_S3_BUCKET=story-game-dev-20250126
# 리전 (서울: ap-northeast-2)
AWS_S3_REGION=ap-northeast-2
# IAM 사용자 액세스 키 (AKIA로 시작, 20자)
AWS_ACCESS_KEY=AKIA...
# IAM 사용자 시크릿 키 (40자)
AWS_SECRET_KEY=...

# ==============================================
# JWT 설정
# ==============================================
# 256비트 이상 시크릿 키 (프로덕션에서는 반드시 변경!)
JWT_SECRET=your-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm-please-change-this-in-production
# 액세스 토큰 유효기간 (밀리초, 기본: 1시간)
JWT_ACCESS_TOKEN_VALIDITY=3600000
# 리프레시 토큰 유효기간 (밀리초, 기본: 7일)
JWT_REFRESH_TOKEN_VALIDITY=604800000

# ==============================================
# 데이터베이스 설정
# ==============================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=story_game
DB_USERNAME=root
DB_PASSWORD=your_database_password

# ==============================================
# Spring Profile 설정
# ==============================================
# local: 로컬스택 S3 사용
# dev: 개발 환경
# prod: 운영 환경
SPRING_PROFILES_ACTIVE=dev

# ==============================================
# Python AI 서버 설정
# ==============================================
AI_SERVER_URL=http://localhost:8000
AI_SERVER_TIMEOUT=600000

# ==============================================
# Relay 서버 설정 (이미지 생성 중계)
# ==============================================
RELAY_SERVER_URL=http://localhost:8081
RELAY_SERVER_TIMEOUT=30000

# ==============================================
# 서버 설정
# ==============================================
# 서버 포트 (기본값: 8080)
SERVER_PORT=8080

# ==============================================
# JPA 설정
# ==============================================
# create: 매번 테이블 재생성
# update: 스키마 자동 업데이트 (개발용)
# validate: 스키마 검증만
# none: 아무것도 하지 않음 (운영용)
JPA_DDL_AUTO=update

# ==============================================
# CORS 설정
# ==============================================
# 프론트엔드 주소 (콤마로 구분)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ==============================================
# 로그 설정
# ==============================================
# 로그 레벨 (TRACE, DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# ==============================================
# ⚠️ 보안 주의사항
# ==============================================
# 1. 이 파일(.env.example)은 예시입니다
# 2. 실제 값은 .env 파일에 작성하세요
# 3. .env 파일은 절대 Git에 커밋하지 마세요!
# 4. .gitignore에 .env가 포함되어 있는지 확인하세요
